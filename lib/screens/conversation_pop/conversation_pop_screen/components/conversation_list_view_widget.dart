part of '../conversation_pop_screen.dart';

class ConversationListViewWidget extends StatelessWidget {
  const ConversationListViewWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
